import React from "react";
import App from "../client-UI/App";

const Home = () => {
  return (
    <div>
      {/*  Day la home component abcd
      <button onClick={() => console.log("CLIENT chào đằng ấy nha")}>
        CLICK AND LOG TO SERVER SIDE
      </button> */}
      <App />
    </div>
  );
};

export default Home;
